# Server
PORT=3001
NODE_ENV=development

# PostgreSQL Database
# For local development
PG_HOST=localhost
PG_PORT=5432
PG_DATABASE=personal_blog
PG_USER=your_username
PG_PASSWORD=your_password

# For Zeabur (auto-injected, for reference only)
# POSTGRES_HOST=host
# POSTGRES_PORT=5432
# POSTGRES_DBNAME=database_name
# POSTGRES_USERNAME=username
# POSTGRES_PASSWORD=password
# DATABASE_URL=postgresql://username:password@host:port/database

# For Railway (auto-injected, for reference only)
# DATABASE_URL=postgresql://user:password@host:port/database

# SSL for cloud databases (uncomment for production)
# PG_SSL=true

# JWT
JWT_SECRET=please-change-this-to-a-secure-random-string
JWT_EXPIRES_IN=7d

# CORS
# Local development
CORS_ORIGIN=http://localhost:5173
# Production (replace with your frontend URL)
# CORS_ORIGIN=https://your-frontend-domain.com

# Zhipu AI (智谱 AI)
# 请在 https://open.bigmodel.cn/ 获取 API Key
ZHIPUAI_API_KEY=your_api_key_here
